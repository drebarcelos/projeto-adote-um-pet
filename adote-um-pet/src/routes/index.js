import { Routes, Route, BrowserRouter } from 'react-router-dom'
export const AppRoutes = () => {
    return (
        <BrowserRouter>
            <Routes>
                <Route/>
            </Routes>
        </BrowserRouter>
    )
}